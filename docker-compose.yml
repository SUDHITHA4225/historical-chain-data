version: '3.8'
services:
  hardhat:
    image: node:20
    working_dir: /app
    command: sh -c "npm install && npx hardhat node"
    ports:
      - "8545:8545"
    volumes:
      - .:/app
    environment:
      NODE_ENV: development
